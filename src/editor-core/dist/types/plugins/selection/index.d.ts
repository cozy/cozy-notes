import { EditorPlugin } from '../../types';
import { SelectionPluginOptions } from './types';
export declare const selectionPlugin: (options?: SelectionPluginOptions | undefined) => EditorPlugin;
export default selectionPlugin;
